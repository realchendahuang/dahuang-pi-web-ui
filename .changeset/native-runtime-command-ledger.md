---
"@realchendahuang/dahuang-pi-web-ui": patch
---

Persist native Runtime command receipts across restarts so an interrupted retry cannot silently repeat a session, terminal, Git, or workspace side effect.
