---
"@realchendahuang/dahuang-pi-web-ui": patch
---

Add the native macOS Pi Agent app shell: project and thread navigation, transcript, SwiftTerm terminal, and workspace panels over a supervised bundled Pi Runtime, with a menu-bar entry to reopen windows.
